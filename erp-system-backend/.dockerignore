target/
.git
.gitignore
.idea
*.iml
# Keep .mvn/wrapper/maven-wrapper.jar — Nixpacks/Railway use a Docker build that honors
# .dockerignore; excluding the jar breaks `mvnw` (MavenWrapperMain not found).