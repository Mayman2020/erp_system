# Thin wrapper — stack lives in ops/docker (single source of truth).
# From repo root: docker compose up --build
# Env: ops/environment/.env or repository-root .env

include:
  - path: ops/docker/docker-compose.yml
